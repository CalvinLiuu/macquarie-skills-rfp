[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "macquarie-skills-rfp"
version = "0.1.0"
description = "GitHub Copilot RFP skills and SharePoint knowledge pipeline scaffold."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["rfp_copilot"]
